/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Adjust if you need custom base path or asset prefix
  // basePath: '',
  // assetPrefix: '',
}

module.exports = nextConfig
